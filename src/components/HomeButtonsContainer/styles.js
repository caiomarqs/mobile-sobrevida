import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    homeButtonsContanier: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap'
    }
})

export { styles }